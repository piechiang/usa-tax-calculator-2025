/**
 * Error handling components
 */

export { ErrorBoundary, withErrorBoundary } from './ErrorBoundary';
export { errorLogger } from '../../utils/errorLogger';
export type { ErrorLog } from '../../utils/errorLogger';
